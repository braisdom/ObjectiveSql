# To take a new SQL experience in Java
How is the ObjectiveSql, how does it stand out from the many ORM frameworks?  Java is a static language although, it has provided the ability to extend code dynamically since version 1.6, Makes the same as Ruby, Python, Javascript and other dynamic languages, except that it provides extension point at compile time

